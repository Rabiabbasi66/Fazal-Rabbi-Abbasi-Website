@'
{
    "functions": {
        "app/main.py": {
            "runtime": "python@3.0.0"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app/main.py"
        }
    ]
}
'@ | Out-File -FilePath backend/vercel.json -Encoding utf8